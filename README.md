# shadow-render-template
A template starting point for the shadow-render toolkit

### First Steps

  ``` npm install ```
  
  ``` yarn install ```
 
#### To run the app
  
 ``` yarn serve ```
 
 ``` npm run serve ```

That's it . You have a working shadow-render application. :tada:

  
